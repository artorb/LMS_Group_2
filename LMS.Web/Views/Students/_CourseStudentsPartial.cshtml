@model IEnumerable<Lms.Core.Models.ViewModels.StudentCommonCourseViewModel>

<br />
<h4>Participants</h4>

<section class="container">
    <section class="row">

        <table class="table table-hover">
            <thead>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.StudentName)         </th>
                    <th> @Html.DisplayNameFor(model => model.Email)    </th>
                </tr>
            </thead>
            <tbody>
                @{int i = 1;}
                @foreach (var item in Model)
                {
                <tr>
                    <td><span>@i. </span>  @Html.DisplayFor(modelItem => item.StudentName)  </td>
                    <td> @Html.DisplayFor(modelItem => item.Email)               </td>
                    @if (User.IsInRole("Teacher"))
                    {
                        <td> <a class="btn btn-sm btn-success" asp-controller="Students" asp-action="Edit" asp-route-Id = "@item.Id">Edit</a></td>
                        <td> <a class="btn btn-sm btn-danger" asp-controller="Students" asp-action="Delete" asp-route-Id="@item.Id">Delete</a></td>
                       
                    }
                </tr>
                    {
                        i++;
                    }
                }
            </tbody>
        </table>

    </section>
</section>
